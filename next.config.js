/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'https://didactoysperu.com', 'localhost'],
    },
};

module.exports = nextConfig;
